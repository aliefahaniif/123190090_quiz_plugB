

import 'package:flutter/material.dart';

class listLagu extends StatelessWidget {
  const listLagu({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
        title: Text("  "),
    ),
    );
  }
}